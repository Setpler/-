'use strict';

exports.__esModule = true;
exports.globalOptions = undefined;

var _options = require('./utils/options');

var options = _interopRequireWildcard(_options);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

class GlobalOptionsStore {
  constructor() {
    this.store = {
      disableCmdPrefix: undefined
    };
  }

  set(key, value) {
    this.store[key] = value;
  }
  get(key) {
    return this.store[key];
  }
  getAll() {
    return this.store;
  }
  setFromFlags(flags) {
    if (flags.prefix === false) {
      this.set('disableCmdPrefix', true);
    }
  }
} /**
   * Singleton that manages global bolt options.
   */

const globalOptions = exports.globalOptions = new GlobalOptionsStore();