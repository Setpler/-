'use strict';

exports.__esModule = true;
exports.promiseWrapper = promiseWrapper;
exports.promiseWrapperSuccess = promiseWrapperSuccess;
function promiseWrapper(promiseFunc) {
  return function () {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return promiseFunc.apply(undefined, args).then(function (result) {
      return { result, status: 'success', args };
    }, function (error) {
      return { error, status: 'error', args };
    });
  };
}

function promiseWrapperSuccess(promiseFunc) {
  return function () {
    for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }

    return promiseFunc.apply(undefined, args).then(function (result) {
      return {
        result,
        status: 'success',
        args
      };
    });
  };
}